# Vision Verification Toolkit

機器視覺驗證工具集（C# WinForms 版）

## 專案目標

開發一個具有圖形化操作介面的 Windows 桌面應用程式，作為機器視覺演算法的快速驗證工具。提供完整的影像前處理、物件檢測、幾何量測、以及參數自動搜尋功能，協助使用者快速評估不同演算法與參數組合的效果。

## 技術堆疊

- .NET 7.0 (Windows)
- WinForms（圖形介面）
- OpenCvSharp4（影像處理）
- MathNet.Numerics（數值計算、曲線擬合）
- OxyPlot.WindowsForms（圖表繪製）

## NuGet 套件

```
OpenCvSharp4
OpenCvSharp4.runtime.win
MathNet.Numerics
OxyPlot.WindowsForms
```

---

## 整體架構

### 三頁面設計

```
┌─────────────────────────────────────────────────────────────────────────┐
│  [頁面1: 前處理]    [頁面2: 檢測/量測]    [頁面3: 參數搜尋]              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│                            各頁面內容區                                  │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│  狀態列：[影像資訊] [滑鼠座標] [處理時間] [記憶體使用]                    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 資料流

```
載入影像 → 頁面1(前處理) → 頁面2(檢測/量測) → 匯出結果
                ↑                ↑
                └── 頁面3(參數搜尋) ──┘
```

---

# 頁面1：前處理

## 介面佈局

```
┌─────────────────────────────────────────────────────────────────────────┐
│  [頁面1: 前處理]    [頁面2: 檢測/量測]    [頁面3: 參數搜尋]              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─ 工具列 ───────────────────────────────────────────────────────────┐ │
│  │ [載入影像] [儲存處理結果] [重置] [常用預設 ▼] [傳送至檢測頁面]      │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌─ 影像顯示區 ───────────────────────────────────────────────────────┐ │
│  │  ┌──────────────────────┐    ┌──────────────────────┐              │ │
│  │  │                      │    │                      │              │ │
│  │  │       原圖           │    │     處理後影像        │              │ │
│  │  │                      │    │                      │              │ │
│  │  │                      │    │                      │              │ │
│  │  └──────────────────────┘    └──────────────────────┘              │ │
│  │         [100%] [適合視窗]          [100%] [適合視窗] [同步縮放 ☑]   │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌─ 影像品質指標（即時） ──────────────────────────────────────────────┐ │
│  │  銳利度：[████████░░] 523    對比度：[██████░░░░] 45                │ │
│  │  亮度：  [█████░░░░░] 128    雜訊：  [██░░░░░░░░] 低                │ │
│  │  動態範圍：[███████░░░] 186   整體評價：[良好 - 建議輕微銳化]        │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌─ 處理管線 (Pipeline) ───────────────────────────────────────────────┐ │
│  │                                                                     │ │
│  │  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐            │ │
│  │  │ 高斯濾波 │ → │ 二值化   │ → │ 形態學   │ → │ [+新增]  │            │ │
│  │  │  σ=1.5  │   │ Otsu    │   │ 開運算   │   │         │            │ │
│  │  └────┬────┘   └────┬────┘   └────┬────┘   └─────────┘            │ │
│  │       ↓啟用         ↓啟用         ↓停用                             │ │
│  │  [上移][下移][刪除][複製]    [清空管線][儲存管線][載入管線]          │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌─ 當前步驟參數 ──────────────────────────────────────────────────────┐ │
│  │                                                                     │ │
│  │  步驟類型：[高斯濾波 ▼]                                             │ │
│  │                                                                     │ │
│  │  核大小：  [3 ▼]  (3/5/7/9/11)                                      │ │
│  │  Sigma X： [═══●═══════] 1.5                                        │ │
│  │  Sigma Y： [═══●═══════] 1.5   [☑ 與X相同]                          │ │
│  │                                                                     │ │
│  │  [套用] [即時預覽 ☑]                                                │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 功能規格

### 1.1 影像載入

- 支援格式：PNG、JPG、BMP、TIFF
- 拖放檔案載入
- 自動計算並顯示影像品質指標
- 支援縮放、平移、左右同步

### 1.2 影像品質即時評估

程式載入影像後自動計算以下指標：

| 指標 | 計算方式 | 判斷標準 |
|------|----------|----------|
| 銳利度 | Laplacian 變異數 | <100 模糊(紅), 100-300 普通(黃), >300 清晰(綠) |
| 對比度 | 灰階標準差 | <30 低(紅), 30-60 中(黃), >60 高(綠) |
| 亮度 | 平均灰階值 | <50 過暗(紅), 50-200 正常(綠), >200 過亮(紅) |
| 雜訊程度 | 高頻能量比例 | 高/中/低 三級 |
| 動態範圍 | max - min | <100 窄(黃), >100 寬(綠) |

顯示整體評價與建議（例如：「建議先進行直方圖等化」）

### 1.3 處理管線 (Pipeline)

支援多步驟串接，每個步驟可獨立啟用/停用。

#### 可用步驟類型

**濾波類**

| 步驟 | 參數 |
|------|------|
| 高斯濾波 | 核大小 (3-15), σX, σY |
| 中值濾波 | 核大小 (3-15) |
| 雙邊濾波 | d, σColor, σSpace |
| 均值濾波 | 核大小 |
| 方框濾波 | 核大小, normalize |

**二值化類**

| 步驟 | 參數 |
|------|------|
| 固定閾值 | 閾值 (0-255), 類型 (Binary/Inv/Trunc/ToZero) |
| Otsu | 自動計算閾值 |
| 自適應閾值 | 方法 (Mean/Gaussian), 區塊大小, C值 |
| 雙閾值 | 低閾值, 高閾值 |

**形態學類**

| 步驟 | 參數 |
|------|------|
| 膨脹 | 結構元素 (形狀/大小), 迭代次數 |
| 侵蝕 | 結構元素 (形狀/大小), 迭代次數 |
| 開運算 | 結構元素, 迭代次數 |
| 閉運算 | 結構元素, 迭代次數 |
| 形態學梯度 | 結構元素, 類型 (標準/內部/外部/組合) |
| Top-Hat | 結構元素 (白/黑) |

**邊緣檢測類**

| 步驟 | 參數 |
|------|------|
| Canny | 低閾值, 高閾值, aperture |
| Sobel | 方向 (X/Y/Both), 核大小 |
| Laplacian | 核大小 |
| 形態學邊緣 | 結構元素大小, 梯度類型 |
| 通用邊緣 | 灰度差閾值, 最小邊緣點數 |

**增強類**

| 步驟 | 參數 |
|------|------|
| 直方圖等化 | 無參數 |
| CLAHE | clipLimit, tileSize |
| 銳化 | 強度 |
| 對比度拉伸 | α, β |
| Gamma校正 | γ值 |

**方向性檢測類**

| 步驟 | 參數 |
|------|------|
| 水平邊緣 | 結構元素長度 |
| 垂直邊緣 | 結構元素長度 |
| 對角線邊緣 | 角度 (45°/135°), 結構元素長度 |
| 多方向組合 | 權重分配 |

### 1.4 常用預設模式

一鍵套用預設的 Pipeline 組合：

| 預設名稱 | 管線組合 | 適用場景 |
|----------|----------|----------|
| 標準邊緣檢測 | 高斯(σ=1.5) → Canny(50,150) | 一般邊緣檢測 |
| 低對比度影像 | CLAHE → 高斯 → 自適應二值化 | 對比度不足的影像 |
| 高雜訊環境 | 中值(5) → 雙邊 → 形態學開運算 | 雜訊較多的工業環境 |
| 微小物體 | 高斯(σ=0.5) → 形態學梯度(3) | 3×3 等微小目標 |
| 模糊邊界 | CLAHE → 銳化 → Canny | 邊界不清晰的物體 |
| 高對比快速 | 固定閾值(Otsu) → 形態學閉運算 | 對比度高的簡單場景 |

### 1.5 管線管理

- 儲存管線：將當前 Pipeline 設定存成 JSON 檔案
- 載入管線：從檔案載入 Pipeline 設定
- 複製步驟：複製選中的步驟
- 步驟排序：上移/下移調整順序
- 啟用/停用：單獨控制每個步驟是否執行

---

# 頁面2：檢測/量測

## 介面佈局

```
┌─────────────────────────────────────────────────────────────────────────┐
│  [頁面1: 前處理]    [頁面2: 檢測/量測]    [頁面3: 參數搜尋]              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─ 工具列 ───────────────────────────────────────────────────────────┐ │
│  │ [使用原圖] [使用處理後影像] [校正設定] [清除所有物件] [匯出結果]    │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌────────┬────────────────────────────────────┬───────────────────────┐ │
│  │        │                                    │                       │ │
│  │ 工具   │                                    │  物件列表             │ │
│  │ 面板   │                                    │  (TreeView)           │ │
│  │        │        影像顯示區                   │                       │ │
│  │┌──────┐│                                    │  ├─ 圓形              │ │
│  ││ 檢測 ││        - 顯示影像                   │  │  ├─ Circle_001    │ │
│  │├──────┤│        - 顯示檢測物件               │  │  └─ Circle_002    │ │
│  ││找圓   ││        - 顯示量測標註               │  ├─ 直線              │ │
│  ││找線   ││        - 滑鼠互動                   │  │  └─ Line_001      │ │
│  ││找點   ││                                    │  ├─ 虛擬物件          │ │
│  ││找輪廓 ││                                    │  │  └─ Point_001     │ │
│  ││能量輪廓│                                    │  └─ 量測結果          │ │
│  │├──────┤│                                    │     ├─ Dist_001      │ │
│  ││ 建構 ││                                    │     └─ Angle_001     │ │
│  │├──────┤│                                    │                       │ │
│  ││兩線交點│                                    ├───────────────────────┤ │
│  ││平行線 ││                                    │  物件詳細資訊         │ │
│  ││垂直線 ││                                    │  (PropertyGrid)       │ │
│  ││圓心連線│                                    │                       │ │
│  │├──────┤│                                    │  名稱：Circle_001     │ │
│  ││ 量測 ││                                    │  圓心X：256.37 px     │ │
│  │├──────┤│                                    │  圓心Y：198.16 px     │ │
│  ││距離   ││                                    │  半徑：104.52 px      │ │
│  ││角度   ││                                    │  直徑：209.05 px      │ │
│  │└──────┘│                                    │  R²：0.998            │ │
│  │        │                                    │                       │ │
│  └────────┴────────────────────────────────────┴───────────────────────┘ │
│                                                                         │
│  ┌─ 工具選項 ──────────────────────────────────────────────────────────┐ │
│  │  （根據選擇的工具動態顯示對應參數）                                  │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 功能規格

### 2.1 像素解析度校正

選單 [校正設定]：

| 校正方式 | 操作 |
|----------|------|
| 手動輸入 | 直接輸入 mm/pixel 或 pixel/mm |
| 標準圓校正 | 檢測圓 → 輸入實際直徑 → 自動計算 |
| 標準距離校正 | 量測兩點 → 輸入實際距離 → 自動計算 |

### 2.2 檢測工具

#### 找圓

**操作**：框選 ROI → 自動檢測圓形

**方法選項**：

| 方法 | 參數 | 特點 |
|------|------|------|
| 霍夫圓 | dp, minDist, param1, param2, minRadius, maxRadius | 快速，適合標準圓 |
| 邊緣擬合 | Canny閾值, 擬合點數, RANSAC | 精度高，適合部分圓弧 |

**輸出物件**：CircleObject（圓心X, 圓心Y, 半徑, 直徑, R²）

#### 找線

**操作**：畫搜尋線段 → 沿垂直方向檢測邊緣 → 擬合直線

**方法選項**：

| 方法 | 參數 | 特點 |
|------|------|------|
| 卡尺法 | 採樣點數, 剖面長度, 邊緣極性, 閾值 | 工業標準做法 |
| 霍夫線 | threshold, minLength, maxGap | 快速檢測 |
| 邊緣擬合 | 亞像素方法選擇 | 高精度 |

**亞像素選項**：拋物線擬合 / 高斯擬合 / 矩法 / Sigmoid擬合

**輸出物件**：LineObject（起點, 終點, 斜率, 截距, 角度, 長度, R²）

#### 找點

**操作**：框選 ROI → 檢測特徵點

**方法選項**：

| 方法 | 參數 | 特點 |
|------|------|------|
| Harris 角點 | blockSize, ksize, k | 經典角點檢測 |
| Shi-Tomasi | maxCorners, quality, minDistance | 選取最佳角點 |
| 斑點質心 | 面積範圍, 圓度 | 適合圓形標記點 |

**輸出物件**：PointObject（X, Y, 強度/品質）

#### 找輪廓

**操作**：框選 ROI → 檢測連通輪廓

**參數**：
- 輪廓模式：External / List / CComp / Tree
- 輪廓近似：None / Simple / TC89_L1 / TC89_KCOS
- 面積過濾：最小/最大面積
- 形狀過濾：圓度、長寬比

**輸出物件**：ContourObject（點集, 面積, 周長, 邊界框, 質心, 圓度）

#### 能量最小化輪廓

**操作**：框選 ROI + 指定種子點 → 全局優化找封閉輪廓

**參數**：
- DataWeight：數據項權重 (0.1-10.0)
- SmoothWeight：平滑項權重 (0.0-5.0)
- AnglePenalty：角度懲罰 (0.1-10.0)
- SearchRange：搜尋範圍 (1-20)
- AngleResolution：角度解析度 (36-720)
- ClosurePenalty：封閉懲罰 (1.0-50.0)

**輸出物件**：ContourObject（封閉輪廓點集）

**適用場景**：單一物體、邊界模糊但需要連續封閉輪廓

### 2.3 虛擬物件建構

從已檢測的物件建構新物件：

| 建構類型 | 輸入 | 輸出 | 說明 |
|----------|------|------|------|
| 兩線交點 | Line + Line | Point | 兩直線的交點 |
| 平行線 | Line + 距離 | Line | 過指定距離的平行線 |
| 垂直線 | Line + Point | Line | 過點的垂直線 |
| 圓心連線 | Circle + Circle | Line | 連接兩圓心 |
| 切線 | Circle + Point | Line | 過點對圓的切線 |
| 中點 | Point + Point | Point | 兩點的中點 |
| 圓心 | Circle | Point | 提取圓心為獨立點 |

### 2.4 量測工具

選取物件後，根據物件類型組合提供量測選項：

#### 距離量測

| 組合 | 量測項目 |
|------|----------|
| Point + Point | 直線距離、水平距離、垂直距離 |
| Point + Line | 垂直距離、沿線距離 |
| Point + Circle | 到圓心距離、到邊緣距離 |
| Line + Line | 平行距離（若平行）、交點（若相交） |
| Circle + Circle | 圓心距、邊緣最近距離、邊緣最遠距離 |
| Circle + Line | 圓心到線距離、切點/交點 |

#### 角度量測

| 組合 | 量測項目 |
|------|----------|
| Line + Line | 夾角、補角 |
| Point + Point + Point | 三點定義的夾角 |
| Line + 水平線 | 與水平方向的夾角 |
| Line + 垂直線 | 與垂直方向的夾角 |

#### 幾何特性量測

| 物件 | 量測項目 |
|------|----------|
| Circle | 圓度（擬合殘差） |
| Line | 直線度（擬合殘差） |
| Contour | 面積、周長、圓度、凸度 |
| Circle + Circle | 同心度（圓心偏移量） |
| Line + Line | 平行度、垂直度 |

### 2.5 結果管理

- **物件列表**：樹狀顯示所有檢測物件與量測結果
- **物件操作**：選取、刪除、重新命名、隱藏/顯示
- **高亮顯示**：選取物件時在影像上高亮
- **多選**：支援多選進行批次量測或刪除

### 2.6 匯出功能

| 匯出格式 | 內容 |
|----------|------|
| CSV | 所有物件參數與量測結果 |
| JSON | 完整物件資料（可重新載入） |
| 影像 (PNG) | 含標註的影像截圖 |
| 報告 (HTML) | 影像 + 數據 + 統計的完整報告 |

---

# 頁面3：參數搜尋

## 介面佈局

```
┌─────────────────────────────────────────────────────────────────────────┐
│  [頁面1: 前處理]    [頁面2: 檢測/量測]    [頁面3: 參數搜尋]              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─ 搜尋設定 ──────────────────────────────────────────────────────────┐ │
│  │                                                                     │ │
│  │  搜尋目標：[找圓 (含前處理) ▼]                                       │ │
│  │                                                                     │ │
│  │  ┌─ 前處理參數範圍 ───────────────────────────────────────────────┐ │ │
│  │  │  ☑ 高斯模糊 σ    ：[1.0 ] ~ [5.0 ]  步進 [0.5 ]               │ │ │
│  │  │  ☑ 二值化閾值     ：[50  ] ~ [200 ]  步進 [25  ]               │ │ │
│  │  │  ☐ 形態學核大小   ：[3   ] ~ [7   ]  步進 [2   ]               │ │ │
│  │  └───────────────────────────────────────────────────────────────┘ │ │
│  │                                                                     │ │
│  │  ┌─ 檢測參數範圍 ───────────────────────────────────────────────┐  │ │
│  │  │  ☑ Canny 低閾值   ：[30  ] ~ [100 ]  步進 [10  ]               │ │ │
│  │  │  ☑ Canny 高閾值   ：[100 ] ~ [250 ]  步進 [25  ]               │ │ │
│  │  │  ☑ 霍夫圓 param2  ：[20  ] ~ [50  ]  步進 [5   ]               │ │ │
│  │  │  ☐ 最小半徑       ：[10  ] ~ [50  ]  步進 [10  ]               │ │ │
│  │  └───────────────────────────────────────────────────────────────┘ │ │
│  │                                                                     │ │
│  │  預估組合數：[486] 組    預估時間：[~49 秒]                          │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌─ 評估方式 ──────────────────────────────────────────────────────────┐ │
│  │                                                                     │ │
│  │  評估類型：[● 數量符合] [○ 品質最佳] [○ 位置符合] [○ 複合指標]       │ │
│  │                                                                     │ │
│  │  ┌─ 數量符合設定 ─────────────────────────────────────────────────┐ │ │
│  │  │  預期檢測數量：[1    ]                                          │ │ │
│  │  │  允許誤差：    [± 0  ]                                          │ │ │
│  │  └───────────────────────────────────────────────────────────────┘ │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌─ 搜尋控制 ──────────────────────────────────────────────────────────┐ │
│  │  搜尋方法：[● Grid Search] [○ Random Search] [○ 早停優化]           │ │
│  │  [▶ 開始搜尋]  [⏹ 停止]  進度：[████████░░░░░░░░░░░░] 42%          │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌─ 搜尋結果 ──────────────────────────────────────────────────────────┐ │
│  │                                                                     │ │
│  │  排名│ 評分  │ σ   │閾值 │Canny低│Canny高│param2│檢測數│ 預覽      │ │
│  │  ────┼───────┼─────┼─────┼───────┼───────┼──────┼──────┼────────   │ │
│  │  1   │ 1.000 │ 2.0 │ 125 │ 50    │ 150   │ 30   │ 1    │ [查看]    │ │
│  │  2   │ 0.950 │ 2.5 │ 125 │ 50    │ 150   │ 30   │ 1    │ [查看]    │ │
│  │  3   │ 0.920 │ 2.0 │ 100 │ 50    │ 175   │ 35   │ 1    │ [查看]    │ │
│  │  4   │ 0.880 │ 1.5 │ 150 │ 60    │ 175   │ 30   │ 1    │ [查看]    │ │
│  │  ...                                                                │ │
│  │                                                                     │ │
│  │  [套用排名1至前處理] [套用排名1至檢測] [匯出結果CSV] [參數分布圖]    │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 功能規格

### 3.1 搜尋目標

可選擇的搜尋目標：

| 目標 | 涵蓋參數 |
|------|----------|
| 找圓（含前處理） | 前處理 Pipeline + 霍夫圓/邊緣擬合參數 |
| 找圓（僅檢測） | 僅霍夫圓/邊緣擬合參數 |
| 找線（含前處理） | 前處理 Pipeline + 卡尺法參數 |
| 找線（僅檢測） | 僅卡尺法參數 |
| 邊緣檢測 | 前處理 Pipeline + Canny/形態學邊緣參數 |
| 自訂 | 使用者自選參數組合 |

### 3.2 參數範圍設定

每個參數可設定：
- 起始值
- 結束值
- 步進值
- 啟用/停用

自動計算總組合數與預估時間。

### 3.3 評估方式

#### 方式1：數量符合

```
設定：
- 預期檢測數量：N
- 允許誤差：±M

評分 = 1.0 - |實際數量 - 預期數量| / 預期數量
若誤差超過允許範圍，評分 = 0
```

#### 方式2：品質最佳

```
設定：
- 品質指標：R²（擬合品質）/ 邊緣強度 / 輪廓完整度

評分 = 品質指標值（標準化到 0-1）
```

#### 方式3：位置符合

```
設定：
- 標記 Ground Truth：在影像上手動標記預期位置
  - 點：點擊標記
  - 圓：拖曳畫圓
  - 線：拖曳畫線

評分 = 1.0 - (位置誤差 / 容許誤差)
```

#### 方式4：複合指標

```
設定：
- 選擇多個指標
- 設定各指標權重

評分 = Σ(權重i × 指標i) / Σ權重i
```

#### 方式5：形狀符合

```
設定：
- 預期形狀：圓形 / 矩形 / 線條
- 預期尺寸範圍

評分 = 形狀匹配度 × 尺寸匹配度
```

#### 方式6：穩定性評估

```
設定：
- 對同一影像加入微小擾動（雜訊、位移）
- 重複檢測多次

評分 = 結果一致性（標準差的倒數）
```

### 3.4 搜尋方法

| 方法 | 特點 | 適用場景 |
|------|------|----------|
| Grid Search | 窮舉所有組合 | 參數空間小（<1000組） |
| Random Search | 隨機採樣 | 參數空間大，快速探索 |
| 早停優化 | 找到符合條件就停止 | 只需要一個可行解 |

### 3.5 搜尋結果

- 依評分排序顯示
- 可預覽每組參數的檢測結果
- 一鍵套用至前處理或檢測頁面
- 匯出完整結果（CSV）
- 參數分布圖：視覺化哪些參數範圍效果較好

---

# 專案結構

```
VisionVerificationToolkit/
├── VisionVerificationToolkit.sln
├── VisionVerificationToolkit/
│   ├── Program.cs
│   ├── MainForm.cs
│   ├── MainForm.Designer.cs
│   │
│   ├── Pages/
│   │   ├── PreprocessingPage.cs       # 頁面1：前處理
│   │   ├── DetectionPage.cs           # 頁面2：檢測/量測
│   │   └── ParameterSearchPage.cs     # 頁面3：參數搜尋
│   │
│   ├── Dialogs/
│   │   ├── CalibrationDialog.cs       # 校正設定
│   │   ├── GroundTruthDialog.cs       # Ground Truth 標記
│   │   ├── PipelineSaveDialog.cs      # 管線儲存
│   │   └── ExportDialog.cs            # 匯出設定
│   │
│   ├── Core/
│   │   ├── ImageManager.cs            # 影像管理
│   │   ├── CalibrationManager.cs      # 校正管理
│   │   ├── PipelineManager.cs         # 管線管理
│   │   └── ResultManager.cs           # 結果管理
│   │
│   ├── Processing/
│   │   ├── IPipelineStep.cs           # 管線步驟介面
│   │   ├── Steps/
│   │   │   ├── GaussianBlurStep.cs
│   │   │   ├── MedianBlurStep.cs
│   │   │   ├── BilateralFilterStep.cs
│   │   │   ├── ThresholdStep.cs
│   │   │   ├── AdaptiveThresholdStep.cs
│   │   │   ├── MorphologyStep.cs
│   │   │   ├── CannyStep.cs
│   │   │   ├── SobelStep.cs
│   │   │   ├── MorphEdgeStep.cs
│   │   │   ├── HistogramEqualizeStep.cs
│   │   │   ├── CLAHEStep.cs
│   │   │   └── ...
│   │   └── ImageQualityAnalyzer.cs    # 影像品質分析
│   │
│   ├── Detection/
│   │   ├── IDetector.cs               # 檢測器介面
│   │   ├── CircleDetector.cs          # 找圓
│   │   ├── LineDetector.cs            # 找線
│   │   ├── PointDetector.cs           # 找點
│   │   ├── ContourDetector.cs         # 找輪廓
│   │   ├── EnergyContourDetector.cs   # 能量最小化輪廓
│   │   └── SubpixelEdge.cs            # 亞像素邊緣
│   │
│   ├── Objects/
│   │   ├── IGeometryObject.cs         # 幾何物件介面
│   │   ├── PointObject.cs
│   │   ├── LineObject.cs
│   │   ├── CircleObject.cs
│   │   ├── ContourObject.cs
│   │   └── MeasurementResult.cs       # 量測結果
│   │
│   ├── Construction/
│   │   ├── IConstructor.cs            # 建構器介面
│   │   ├── LineIntersection.cs        # 兩線交點
│   │   ├── ParallelLine.cs            # 平行線
│   │   ├── PerpendicularLine.cs       # 垂直線
│   │   └── ...
│   │
│   ├── Measurement/
│   │   ├── IMeasurement.cs            # 量測介面
│   │   ├── DistanceMeasurement.cs     # 距離量測
│   │   ├── AngleMeasurement.cs        # 角度量測
│   │   └── GeometryMeasurement.cs     # 幾何特性量測
│   │
│   ├── Search/
│   │   ├── ISearchStrategy.cs         # 搜尋策略介面
│   │   ├── GridSearchStrategy.cs      # Grid Search
│   │   ├── RandomSearchStrategy.cs    # Random Search
│   │   ├── EarlyStopStrategy.cs       # 早停搜尋
│   │   ├── IEvaluator.cs              # 評估器介面
│   │   ├── CountEvaluator.cs          # 數量評估
│   │   ├── QualityEvaluator.cs        # 品質評估
│   │   ├── PositionEvaluator.cs       # 位置評估
│   │   ├── CompositeEvaluator.cs      # 複合評估
│   │   └── SearchResult.cs            # 搜尋結果
│   │
│   ├── Controls/
│   │   ├── ImageCanvas.cs             # 影像顯示控件
│   │   ├── DualImageCanvas.cs         # 雙影像對比控件
│   │   ├── PipelineEditor.cs          # 管線編輯控件
│   │   ├── ToolPanel.cs               # 工具面板
│   │   ├── ObjectTreeView.cs          # 物件樹狀列表
│   │   ├── PropertyPanel.cs           # 屬性面板
│   │   ├── QualityIndicator.cs        # 品質指標顯示
│   │   └── ParameterRangeEditor.cs    # 參數範圍編輯
│   │
│   ├── Graphics/
│   │   ├── Overlay.cs                 # 繪圖疊加層
│   │   ├── CircleGraphic.cs
│   │   ├── LineGraphic.cs
│   │   ├── PointGraphic.cs
│   │   ├── ContourGraphic.cs
│   │   └── MeasurementGraphic.cs      # 量測標註繪製
│   │
│   └── Utils/
│       ├── MathHelper.cs              # 數學輔助
│       ├── GeometryHelper.cs          # 幾何計算
│       ├── ExportHelper.cs            # 匯出功能
│       ├── JsonSerializer.cs          # JSON 序列化
│       └── Settings.cs                # 應用程式設定
```

---

# 快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| Ctrl+O | 開啟影像 |
| Ctrl+S | 儲存結果 |
| Ctrl+E | 匯出 |
| Ctrl+Z | 復原 |
| Ctrl+Y | 重做 |
| 1 | 切換至頁面1 |
| 2 | 切換至頁面2 |
| 3 | 切換至頁面3 |
| Delete | 刪除選取物件 |
| Escape | 取消目前操作 |
| Space | 切換原圖/處理後影像 |
| Ctrl+滾輪 | 縮放影像 |

---

# 錯誤處理

| 情況 | 處理方式 |
|------|----------|
| 影像載入失敗 | 彈出錯誤訊息，保持原狀態 |
| 管線步驟執行錯誤 | 標記該步驟為錯誤，跳過繼續 |
| 檢測無結果 | 提示調整參數或 ROI |
| 擬合失敗 | 標示失敗，顯示可能原因 |
| 參數搜尋中斷 | 保留已完成的結果 |
| 記憶體不足 | 提示並建議縮小搜尋範圍 |

---

# 未來擴充（Phase 2）

1. **更多檢測工具**：弧、橢圓、矩形、多邊形
2. **深度學習整合**：CNN 物件檢測、語義分割
3. **批次處理**：多影像自動處理
4. **腳本支援**：Python/C# 腳本自訂處理邏輯
5. **多相機支援**：即時影像擷取
6. **3D 量測**：雙目視覺、結構光
7. **報表模板**：自訂報表格式
8. **Bayesian 優化**：更智慧的參數搜尋

---

# License

MIT License
